package mysql

import (
	"testing"
)

func TestName(t *testing.T) {
	//d, _ := New(repository.Config{})
	//_ = d.StoreMetadata(context.Background(),service.TokenizeTextMetadata{RequestId: uuid.New().String(),
	//	GlobalTxId: uuid.New().String(), CreatedDate: time.Now().UTC(),Language: "ENG"})
}
